#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int ipo(int k,int w,vector<int> profit,vector<int> capital){
    bool flag = true;
    while(flag)
    {
        
    }
}
int binarySearch(vector<int> capital,int k)
{
    int s = 0;
    int e = capital.size()-1;
    int mid = s + (e-s)/2;
    while(s<=e){
        if(capital[mid]==k)
    }
}

int main()
{
    vector<int> arr1{1,2,3};
    vector<int> arr2{1,1,1};
    int k=2;
    int w=1;
    cout<<ipo(k,w,arr1,arr2);
}