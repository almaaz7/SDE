#include<iostream>
#include<vector>
using namespace std;
void solve(vector<vector<int>> board)
{
    
}
int main()
{
    int n;
    vector<vector<int>> board(n,vector<int>n, 0);
    int col = 0;
    solve(board,col,n);
}